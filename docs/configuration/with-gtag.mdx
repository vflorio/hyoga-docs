---
sidebar_position: 66
---

# Google Tag Manager

import HyogaExperience from '@site/src/components/HyogaExperience';

Variante placeholder per integrazione Google Tag.

Comportamento atteso (da implementare nel runtime in uno step successivo): se è attiva una variabile di readiness su window, il player aspetta il segnale di tag Google prima di consentire la riproduzione del video.

<HyogaExperience
  title="Google Tag Manager"
  subtitle="Placeholder variant con gate di readiness G-TAG (implementation pending)."
  initialEnvironmentKey="auroraProd"
  environmentKeys={['auroraProd', 'auroraStage', 'sonic']}
  initialVariantKey="withGtag"
  variantKeys={['withGtag']}
  showConfigEditor
/>
